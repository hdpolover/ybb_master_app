import 'package:flutter/material.dart';

class ProgramDetailsTab extends StatelessWidget {
  const ProgramDetailsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
